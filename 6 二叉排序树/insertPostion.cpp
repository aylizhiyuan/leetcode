#include <stdio.h>
#include <vector>
class Solution {
    public: 
        int searchInsert(std::vector<int> &nums,int target){
            int index = -1; //最终返回的下标，若找到则为该元素的下标，否则为需要插入的位置
            int begin  = 0; //搜索区间左端点
            int end = nums.size() - 1;
            while(index == -1){
                
            }
        }   
}